// Kept as a compatibility source file for existing CocoaPods projects that
// previously enumerated this path. DecodeRecoveryState lives in
// FlutterAudioPlayer.swift so projects with stale pod source lists still build.
